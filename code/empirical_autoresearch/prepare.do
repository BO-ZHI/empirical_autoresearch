version 18
clear all
set more off

capture mkdir "output"
capture mkdir "logs"

display "project=sanitized empirical autoresearch workflow"
display "manifest=data/manifest.md"
display "prepare_status=ready"
display "prepare_note=This public harness documents setup without exposing confidential data."
